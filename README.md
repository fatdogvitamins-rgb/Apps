# Apps

A small workspace containing example app scaffolds.

## 📦 Included apps

### CLI (Python)

- Directory: `cli/`
- Run:

```bash
python -m cli.app greet --name Alice
```

### Web (Node + Express)

- Directory: `web/`
- Run:

```bash
cd web
npm install
npm start
```

