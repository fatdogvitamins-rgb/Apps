# CLI App

A small Python command-line tool. Run from the repo root:

```bash
python -m cli.app greet --name Alice
python -m cli.app math --operation add --x 5 --y 7
```

Requirements:

- Python 3.8+
